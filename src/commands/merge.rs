
use colored::*;

pub fn execute(branch: &str, ff_only: bool) -> anyhow::Result<()> {
    println!("{}", "Merging...".cyan());
    println!("{} Merge functionality coming soon", "!".yellow());
    Ok(())
}
